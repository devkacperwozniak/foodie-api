/*
We use an approach where configuration files are placed alongside their dedicated folders,
for example, database.config is in the database folder. At the moment, I don't see the need to define an app.config,
but I leave this file for informational purposes or for future use if something comes up
 */
